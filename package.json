{
  "name": "kwhab-community-discovery",
  "version": "0.1.0",
  "private": true,
  "description": "KW Hab Community Discovery Platform — hackathon MVP",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "db:start": "bash scripts/db.sh start",
    "db:setup": "bash scripts/db.sh setup",
    "db:reset": "bash scripts/db.sh reset",
    "db:psql": "bash scripts/db.sh psql",
    "setup": "npm install && npm run db:start && npm run db:setup",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev:server\" \"npm run dev:web\"",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
